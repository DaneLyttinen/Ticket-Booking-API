package asg.concert.service.common;

public class Config {
    public static final String CLIENT_COOKIE_NAME = "auth";
    // ^ it always irked me when the Lab named this variable 'CLIENT_COOKIE' which, come on, isn't that descriptive - at the least, could be 'CLIENT_COOKIE_NAME' (but isn't client implied? having it there makes it sound like the client might be sending the server a cookie; which obviously isn't what is happening)
}
